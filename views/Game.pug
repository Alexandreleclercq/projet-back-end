doctype html

html(lang='fr')

include includes/head.pug

script(defer src="/js/client.js")

script(src="/socket.io/socket.io.js")

body
  h1 Bienvenue a la piscine #{login.charAt(0).toUpperCase() + login.slice(1)}
  h2 Le premier qui rejoint l'eau gagne !!!
   
  div#win
  
  p#greeting #{login}

  div#fin